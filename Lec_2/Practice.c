#include<stdio.h>
int main()
{
    // Problem No: 1
    //printf("Hi there/\\.\tI hope you are doing great and practicing well.\nNow this is a simple task you need to do.\t Just care for the indentation.\nshow 100%% of sincerity.");

    //=====================================================================
    //=====================================================================

    // Problem No: 2
    //int a=10, b=20;
    //printf("%d %d", b, a);

    //=====================================================================
    //=====================================================================

    // Problem No: 3
    //int a=5;
    //printf("%d",a*2);

    //=====================================================================
    //=====================================================================

    // Problem No: 4
    //int a=5, b=2, sum, biyog, gun;
    //sum = a+b;
    //biyog = a-b;
    //gun = a*b;
    //printf("Sum: %d Minus: %d Multiplay: %d",sum,biyog,gun);

    //=====================================================================
    //=====================================================================

    // Problem No: 5
    /* char w = 'A',x = 'C',y = 'a',z = 'd';
    printf("%c = %d\n%c = %d\n%c = %d\n%c = %d\n",w,w,x,x,y,y,z,z); */


    //=====================================================================
    //=====================================================================

    // Problem No: 6
    /* int x;
    char y;
    scanf("%d%c" ,&x,&y);
    printf("%d", x); */

    return 0;
}
